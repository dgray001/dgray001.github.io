{
  "name": "sjf",
  "version": "0.1.0",
  "description": "SJF website",
  "private": true,
  "scripts": {
    "build": "webpack --config webpack.prod.js",
    "dev": "webpack serve --config webpack.dev.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc --noEmit",
    "lint": "npx eslint src",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "npx prettier src docs --check --write",
    "check": "npm run prettier && npm run tsc && npm run lint"
  },
  "author": "Daniel Gray",
  "license": "ISC"
}
